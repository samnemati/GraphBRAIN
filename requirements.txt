# GraphMind — Python Dependencies
# Install with: pip install -r requirements.txt
#
# PyTorch: install separately first from https://pytorch.org/get-started/locally/
# Then install PyG: https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

# Core scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# PyTorch (install manually matching your CUDA version)
# torch>=2.0.0

# PyTorch Geometric (install after torch — see PyG docs for exact command)
# torch-geometric>=2.4
# torch-scatter
# torch-sparse

# Jupyter
jupyterlab>=4.0
ipykernel

# File handling
openpyxl>=3.1        # reading .xlsx behavioral data
h5py>=3.9            # for HDF5-based .mat files (v7.3+)

# Utilities
tqdm>=4.65           # progress bars
pyyaml>=6.0          # config files
